import { Router } from "express";

const router = Router();

router.get("/reviews", {})
router.post("/reviews", {})
router.put("/reviews/:id", {})
router.delete("/reviews/:id", {})

export default router